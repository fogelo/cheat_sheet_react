import React from "react";
import Keyframes from "./Keyframes/Keyframes";

const Animation = () => {
    return (
        <div>
            <Keyframes/>
        </div>
    );
};

export default Animation;
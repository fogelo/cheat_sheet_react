import React from 'react';
import {Box} from '@mui/material';

const SideBar = () => {
    return (
        <Box bgcolor={'skyblue'} flex={1}>
            sidebar
        </Box>
    );
};

export default SideBar;